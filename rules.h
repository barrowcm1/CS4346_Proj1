#ifdef RULES_H
#define RULES_H

#include <string>
#include <vector>

// Define different attack types
class AttackType {
    PHISHING,
    MALWARE,
    DDOS,
    CYBER_ATTACK,
    MAN_IN_THE_MIDDLE,
    BRUTE_FORCE
};

#endif
